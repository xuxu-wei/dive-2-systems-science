{
  "p17-lorenz-vector": "import math\nimport itertools\nimport numpy as np\n\ndef lorenz_rhs(state, sigma=10., rho=28., beta=8./3.):\n    x,y,z=np.asarray(state,dtype=float)\n    return np.array([sigma*(y-x),x*(rho-z)-y,x*y-beta*z],dtype=float)\n\ndef solve(state, sigma, rho, beta):\n    result=lorenz_rhs(state, sigma, rho, beta)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-candidate-row": "import math\nimport itertools\nimport numpy as np\n\ndef feature_row(state):\n    x,y,z=np.asarray(state,dtype=float)\n    return [1.,float(x),float(y),float(z),float(x*x),float(x*y),float(x*z),float(y*y),float(y*z),float(z*z)]\n\ndef solve(state):\n    result=feature_row(state)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-spiral-vector": "import math\nimport itertools\nimport numpy as np\n\ndef spiral_rhs(state, parameters):\n    \"\"\"Four-parameter tanh vector field; linear damped spiral when c=d=0.\"\"\"\n    x,y=np.asarray(state,dtype=float);a,omega,c,d=parameters\n    return np.array([-a*x-omega*y+c*np.tanh(x),omega*x-a*y+d*np.tanh(y)])\n\ndef solve(state, parameters):\n    result=spiral_rhs(state, parameters)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-spiral-reference": "import math\nimport itertools\nimport numpy as np\n\ndef spiral_exact(initial,a,omega,times):\n    initial=np.asarray(initial,dtype=float);times=np.asarray(times,dtype=float)\n    theta=omega*times;factor=np.exp(-a*times)\n    return np.column_stack([factor*(initial[0]*np.cos(theta)-initial[1]*np.sin(theta)),\n                            factor*(initial[0]*np.sin(theta)+initial[1]*np.cos(theta))])\n\ndef solve(initial, a, omega, times):\n    result=spiral_exact(initial, a, omega, times)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-disjoint-trajectories": "import math\nimport itertools\nimport numpy as np\n\ndef split_audit(train_ids,validation_ids,test_ids):\n    train=set(train_ids);validation=set(validation_ids);test=set(test_ids)\n    overlap=sorted((train&validation)|(train&test)|(validation&test))\n    return [len(overlap)==0,overlap]\n\ndef solve(train_ids, validation_ids, test_ids):\n    result=split_audit(train_ids, validation_ids, test_ids)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-sparse-refit": "import math\nimport itertools\nimport numpy as np\n\ndef stlsq(features, derivatives, threshold=.1, iterations=10):\n    \"\"\"Sequential thresholded least squares with an explicit refit per target.\"\"\"\n    X=np.asarray(features,dtype=float);Y=np.atleast_2d(np.asarray(derivatives,dtype=float))\n    if Y.shape[0]!=X.shape[0]:Y=Y.T\n    coefficients=np.linalg.lstsq(X,Y,rcond=None)[0]\n    for _ in range(iterations):\n        keep=np.abs(coefficients)>=threshold\n        updated=np.zeros_like(coefficients)\n        for column in range(Y.shape[1]):\n            if keep[:,column].any():\n                updated[keep[:,column],column]=np.linalg.lstsq(X[:,keep[:,column]],Y[:,column],rcond=None)[0]\n        coefficients=updated\n    return coefficients\n\ndef solve(features, derivatives, threshold, iterations):\n    result=stlsq(features, derivatives, threshold, iterations)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-state-jacobian": "import math\nimport itertools\nimport numpy as np\n\ndef spiral_state_jacobian(state, parameters):\n    x,y=np.asarray(state,dtype=float);a,omega,c,d=parameters\n    return np.array([[-a+c*(1-np.tanh(x)**2),-omega],\n                     [omega,-a+d*(1-np.tanh(y)**2)]])\n\ndef solve(state, parameters):\n    result=spiral_state_jacobian(state, parameters)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-parameter-jacobian": "import math\nimport itertools\nimport numpy as np\n\ndef spiral_parameter_jacobian(state, parameters):\n    x,y=np.asarray(state,dtype=float)\n    return np.array([[-x,-y,np.tanh(x),0.],[-y,x,0.,np.tanh(y)]])\n\ndef solve(state, parameters):\n    result=spiral_parameter_jacobian(state, parameters)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-noisy-refit": "import math\nimport itertools\nimport numpy as np\n\ndef stlsq(features, derivatives, threshold=.1, iterations=10):\n    \"\"\"Sequential thresholded least squares with an explicit refit per target.\"\"\"\n    X=np.asarray(features,dtype=float);Y=np.atleast_2d(np.asarray(derivatives,dtype=float))\n    if Y.shape[0]!=X.shape[0]:Y=Y.T\n    coefficients=np.linalg.lstsq(X,Y,rcond=None)[0]\n    for _ in range(iterations):\n        keep=np.abs(coefficients)>=threshold\n        updated=np.zeros_like(coefficients)\n        for column in range(Y.shape[1]):\n            if keep[:,column].any():\n                updated[keep[:,column],column]=np.linalg.lstsq(X[:,keep[:,column]],Y[:,column],rcond=None)[0]\n        coefficients=updated\n    return coefficients\n\ndef solve(features, derivatives, threshold, iterations):\n    result=stlsq(features, derivatives, threshold, iterations)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-support-row": "import math\nimport itertools\nimport numpy as np\n\ndef feature_row(state):\n    x,y,z=np.asarray(state,dtype=float)\n    return [1.,float(x),float(y),float(z),float(x*x),float(x*y),float(x*z),float(y*y),float(y*z),float(z*z)]\n\ndef solve(state):\n    result=feature_row(state)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-independent-spiral": "import math\nimport itertools\nimport numpy as np\n\ndef spiral_exact(initial,a,omega,times):\n    initial=np.asarray(initial,dtype=float);times=np.asarray(times,dtype=float)\n    theta=omega*times;factor=np.exp(-a*times)\n    return np.column_stack([factor*(initial[0]*np.cos(theta)-initial[1]*np.sin(theta)),\n                            factor*(initial[0]*np.sin(theta)+initial[1]*np.cos(theta))])\n\ndef solve(initial, a, omega, times):\n    result=spiral_exact(initial, a, omega, times)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-paired-failure": "import math\nimport itertools\nimport numpy as np\n\ndef paired_report(baseline,method,failures):\n    baseline=np.asarray(baseline,dtype=float);method=np.asarray(method,dtype=float)\n    failures=np.asarray(failures,dtype=bool)\n    difference=baseline-method\n    return [float(difference.mean()),float(difference.std(ddof=1)/math.sqrt(len(difference))) if len(difference)>1 else 0.,float(failures.mean())]\n\ndef solve(baseline, method, failures):\n    result=paired_report(baseline, method, failures)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-two-compartment": "import math\nimport itertools\nimport numpy as np\n\ndef clearance_rhs(amounts,inputs,clearance,exchange):\n    first,second=amounts;u1,u2=inputs;k1,k2=clearance\n    return [u1-k1*first-exchange*(first-second),\n            u2-k2*second+exchange*(first-second)]\n\ndef solve(amounts, inputs, clearance, exchange):\n    result=clearance_rhs(amounts, inputs, clearance, exchange)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-transfer-score": "import math\nimport itertools\nimport numpy as np\n\ndef clearance_score(truth,prediction,total_input,total_removed,initial_total,final_total):\n    truth=np.asarray(truth,dtype=float);pred=np.asarray(prediction,dtype=float)\n    rmse=float(np.sqrt(np.mean((truth-pred)**2)))\n    balance=float(abs(final_total-initial_total-total_input+total_removed))\n    negative=int(np.sum(pred<0))\n    return [rmse,balance,negative]\n\ndef solve(truth, prediction, total_input, total_removed, initial_total, final_total):\n    result=clearance_score(truth, prediction, total_input, total_removed, initial_total, final_total)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-paired-conclusion": "import math\nimport itertools\nimport numpy as np\n\ndef paired_report(baseline,method,failures):\n    baseline=np.asarray(baseline,dtype=float);method=np.asarray(method,dtype=float)\n    failures=np.asarray(failures,dtype=bool)\n    difference=baseline-method\n    return [float(difference.mean()),float(difference.std(ddof=1)/math.sqrt(len(difference))) if len(difference)>1 else 0.,float(failures.mean())]\n\ndef solve(baseline, method, failures):\n    result=paired_report(baseline, method, failures)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-final-split": "import math\nimport itertools\nimport numpy as np\n\ndef split_audit(train_ids,validation_ids,test_ids):\n    train=set(train_ids);validation=set(validation_ids);test=set(test_ids)\n    overlap=sorted((train&validation)|(train&test)|(validation&test))\n    return [len(overlap)==0,overlap]\n\ndef solve(train_ids, validation_ids, test_ids):\n    result=split_audit(train_ids, validation_ids, test_ids)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-capstone-lorenz": "import math\nimport itertools\nimport numpy as np\n\ndef lorenz_rhs(state, sigma=10., rho=28., beta=8./3.):\n    x,y,z=np.asarray(state,dtype=float)\n    return np.array([sigma*(y-x),x*(rho-z)-y,x*y-beta*z],dtype=float)\n\ndef solve(state, sigma, rho, beta):\n    result=lorenz_rhs(state, sigma, rho, beta)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-capstone-paired": "import math\nimport itertools\nimport numpy as np\n\ndef paired_report(baseline,method,failures):\n    baseline=np.asarray(baseline,dtype=float);method=np.asarray(method,dtype=float)\n    failures=np.asarray(failures,dtype=bool)\n    difference=baseline-method\n    return [float(difference.mean()),float(difference.std(ddof=1)/math.sqrt(len(difference))) if len(difference)>1 else 0.,float(failures.mean())]\n\ndef solve(baseline, method, failures):\n    result=paired_report(baseline, method, failures)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n",
  "p17-capstone-score": "import math\nimport itertools\nimport numpy as np\n\ndef clearance_score(truth,prediction,total_input,total_removed,initial_total,final_total):\n    truth=np.asarray(truth,dtype=float);pred=np.asarray(prediction,dtype=float)\n    rmse=float(np.sqrt(np.mean((truth-pred)**2)))\n    balance=float(abs(final_total-initial_total-total_input+total_removed))\n    negative=int(np.sum(pred<0))\n    return [rmse,balance,negative]\n\ndef solve(truth, prediction, total_input, total_removed, initial_total, final_total):\n    result=clearance_score(truth, prediction, total_input, total_removed, initial_total, final_total)\n    return result.tolist() if isinstance(result,np.ndarray) else result\n"
}
