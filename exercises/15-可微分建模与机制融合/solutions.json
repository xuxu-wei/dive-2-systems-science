{
  "p15-network-value": "import math\nimport numpy as np\n\ndef scalar_net(x, w, b, v, c):\n    return float(v * math.tanh(w * x + b) + c)\n\ndef solve(x, w, b, v, c):\n    return scalar_net(x, w, b, v, c)\n",
  "p15-network-loss-gradient": "import math\nimport numpy as np\n\ndef scalar_loss_grad(x, y, w, b, v, c):\n    z = w * x + b\n    a = math.tanh(z)\n    prediction = v * a + c\n    error = prediction - y\n    factor = error * v * (1 - a * a)\n    return prediction, .5 * error * error, [factor * x, factor, error * a, error]\n\ndef solve(x, y, w, b, v, c):\n    return scalar_loss_grad(x, y, w, b, v, c)\n",
  "p15-reverse-graph": "import math\nimport numpy as np\n\ndef reverse_tape_grad(x, y, w, b, v, c):\n    \"\"\"Tiny reverse-mode tape; a node can feed more than one downstream edge.\"\"\"\n    class Node:\n        def __init__(self, value, parents=()):\n            self.value = float(value)\n            self.parents = parents\n            self.grad = 0.\n\n    def add(left, right):\n        return Node(left.value+right.value, ((left,1.),(right,1.)))\n\n    def mul(left, right):\n        return Node(left.value*right.value,\n                    ((left,right.value),(right,left.value)))\n\n    def tanh(arg):\n        value = math.tanh(arg.value)\n        return Node(value, ((arg,1-value*value),))\n\n    xnode, ynode = Node(x), Node(y)\n    params = [Node(value) for value in (w,b,v,c)]\n    z = add(mul(params[0],xnode),params[1])\n    pred = add(mul(params[2],tanh(z)),params[3])\n    error = add(pred,Node(-ynode.value))\n    loss = mul(Node(.5),mul(error,error))\n    visited, order = set(), []\n    def visit(node):\n        if id(node) in visited:\n            return\n        visited.add(id(node))\n        for parent,_ in node.parents:\n            visit(parent)\n        order.append(node)\n    visit(loss)\n    loss.grad = 1.\n    for node in reversed(order):\n        for parent,local in node.parents:\n            parent.grad += node.grad*local\n    return pred.value, loss.value, [node.grad for node in params]\n\ndef solve(x, y, w, b, v, c):\n    return reverse_tape_grad(x, y, w, b, v, c)\n",
  "p15-difference-check": "import math\nimport numpy as np\n\ndef scalar_net(x, w, b, v, c):\n    return float(v * math.tanh(w * x + b) + c)\n\n\ndef finite_difference_grad(x, y, w, b, v, c, h):\n    values = [w, b, v, c]\n    gradients = []\n    for j in range(4):\n        plus, minus = values.copy(), values.copy()\n        plus[j] += h\n        minus[j] -= h\n        lp = .5 * (scalar_net(x, *plus) - y)**2\n        lm = .5 * (scalar_net(x, *minus) - y)**2\n        gradients.append((lp - lm) / (2*h))\n    return gradients\n\ndef solve(x, y, w, b, v, c, h):\n    return finite_difference_grad(x, y, w, b, v, c, h)\n",
  "p15-fixed-map": "import math\nimport numpy as np\n\ndef discrete_step(x, u, a, b, q):\n    \"\"\"Fixed sampling interval: x_next = a*x + b*u + q*tanh(x).\"\"\"\n    return float(a*x + b*u + q*math.tanh(x))\n\ndef solve(x, u, a, b, q):\n    return discrete_step(x, u, a, b, q)\n",
  "p15-teacher-forced": "import math\nimport numpy as np\n\ndef discrete_step(x, u, a, b, q):\n    \"\"\"Fixed sampling interval: x_next = a*x + b*u + q*tanh(x).\"\"\"\n    return float(a*x + b*u + q*math.tanh(x))\n\n\ndef teacher_forced(states, inputs, a, b, q):\n    return [discrete_step(float(x), float(u), a, b, q)\n            for x, u in zip(states[:-1], inputs)]\n\ndef solve(states, inputs, a, b, q):\n    return teacher_forced(states, inputs, a, b, q)\n",
  "p15-free-rollout": "import math\nimport numpy as np\n\ndef discrete_step(x, u, a, b, q):\n    \"\"\"Fixed sampling interval: x_next = a*x + b*u + q*tanh(x).\"\"\"\n    return float(a*x + b*u + q*math.tanh(x))\n\n\ndef discrete_roll(x0, inputs, a, b, q):\n    states = [float(x0)]\n    for u in inputs:\n        states.append(discrete_step(states[-1], float(u), a, b, q))\n    return states\n\ndef solve(x0, inputs, a, b, q):\n    return discrete_roll(x0, inputs, a, b, q)\n",
  "p15-rollout-error": "import math\nimport numpy as np\n\ndef rollout_metrics(truth, prediction):\n    \"\"\"Exclude the shared initial state from the mean squared rollout error.\"\"\"\n    diff = np.asarray(prediction[1:],dtype=float)-np.asarray(truth[1:],dtype=float)\n    return [float(np.mean(diff*diff)),float(abs(diff[-1]))]\n\ndef solve(truth, prediction):\n    return rollout_metrics(truth, prediction)\n",
  "p15-exact-decay": "import math\nimport numpy as np\n\ndef clearance_exact(x0, k, times):\n    return [float(x0*math.exp(-k*t)) for t in times]\n\ndef solve(x0, k, times):\n    return clearance_exact(x0, k, times)\n",
  "p15-euler-sensitivity": "import math\nimport numpy as np\n\ndef euler_forward_sensitivity(x0, k, h, steps):\n    \"\"\"Return state and dx/dk for the *discretized* Euler program.\"\"\"\n    states, sensitivity = [float(x0)], [0.]\n    for _ in range(steps):\n        x, s = states[-1], sensitivity[-1]\n        states.append((1-k*h)*x)\n        sensitivity.append((1-k*h)*s-h*x)\n    return states, sensitivity\n\ndef solve(x0, k, h, steps):\n    return euler_forward_sensitivity(x0, k, h, steps)\n",
  "p15-continuous-gradient": "import math\nimport numpy as np\n\ndef continuous_loss_gradient(x0, k, observations):\n    \"\"\"Exact continuous-observation objective and its derivative.\"\"\"\n    loss, gradient = 0., 0.\n    for t, y in observations:\n        x = x0*math.exp(-k*t)\n        residual = x-y\n        loss += .5*residual*residual\n        gradient -= t*x*residual\n    return loss, gradient\n\ndef solve(x0, k, observations):\n    return continuous_loss_gradient(x0, k, observations)\n",
  "p15-discrete-adjoint": "import math\nimport numpy as np\n\ndef euler_forward_sensitivity(x0, k, h, steps):\n    \"\"\"Return state and dx/dk for the *discretized* Euler program.\"\"\"\n    states, sensitivity = [float(x0)], [0.]\n    for _ in range(steps):\n        x, s = states[-1], sensitivity[-1]\n        states.append((1-k*h)*x)\n        sensitivity.append((1-k*h)*s-h*x)\n    return states, sensitivity\n\n\ndef euler_reverse_gradient(x0, k, h, observations):\n    \"\"\"Grid-indexed observations; reverse accumulation differentiates Euler.\"\"\"\n    steps = max(int(i) for i in observations)\n    states, _ = euler_forward_sensitivity(x0, k, h, steps)\n    lam, gradient = 0., 0.\n    for i in range(steps, -1, -1):\n        lam += states[i]-observations[i] if i in observations else 0.\n        if i:\n            gradient -= h*states[i-1]*lam\n            lam *= (1-k*h)\n    return gradient\n\n\ndef euler_reverse_list(x0, k, h, observation_indices, targets):\n    return euler_reverse_gradient(x0,k,h,dict(zip(observation_indices,targets)))\n\ndef solve(x0, k, h, observation_indices, targets):\n    return euler_reverse_list(x0, k, h, observation_indices, targets)\n",
  "p15-trial-derivative": "import math\nimport numpy as np\n\ndef tanh_trial(t, c, v, w, b):\n    z = math.tanh(w*t+b)\n    return c+v*z, v*w*(1-z*z)\n\ndef solve(t, c, v, w, b):\n    return tanh_trial(t, c, v, w, b)\n",
  "p15-ode-loss-parts": "import math\nimport numpy as np\n\ndef tanh_trial(t, c, v, w, b):\n    z = math.tanh(w*t+b)\n    return c+v*z, v*w*(1-z*z)\n\n\ndef ode_loss_parts(times, observed, k, c, v, w, b, initial):\n    \"\"\"Mean data/residual loss and initial-state loss, each kept separate.\"\"\"\n    values = [tanh_trial(float(t), c, v, w, b) for t in times]\n    data = sum((state-float(y))**2 for (state,_),y in zip(values,observed))/len(times)\n    physics = sum((slope+k*state)**2 for state,slope in values)/len(times)\n    boundary = (tanh_trial(0.,c,v,w,b)[0]-initial)**2\n    return [float(data),float(physics),float(boundary)]\n\ndef solve(times, observed, k, c, v, w, b, initial):\n    return ode_loss_parts(times, observed, k, c, v, w, b, initial)\n",
  "p15-diffusion-profile": "import math\nimport numpy as np\n\ndef diffusion_values(points, time, rate, slope):\n    return [float(math.exp(-rate*time)*math.cos(math.pi*x)+slope*x)\n            for x in points]\n\ndef solve(points, time, rate, slope):\n    return diffusion_values(points, time, rate, slope)\n",
  "p15-diffusion-residual": "import math\nimport numpy as np\n\ndef diffusion_parts(points, times, diffusivity, rate, slope):\n    \"\"\"For phi=exp(-rate*t)cos(pi*x)+slope*x on [0,1].\"\"\"\n    residuals = []\n    for x,t in zip(points,times):\n        base = math.exp(-rate*t)*math.cos(math.pi*x)\n        residuals.append((-rate+diffusivity*math.pi**2)*base)\n    pde = sum(value*value for value in residuals)/len(residuals)\n    # Spatial derivatives of the cosine vanish at both boundaries.\n    boundary = slope*slope\n    return [float(pde), float(boundary)]\n\ndef solve(points, times, diffusivity, rate, slope):\n    return diffusion_parts(points, times, diffusivity, rate, slope)\n",
  "p15-hybrid-right-side": "import math\nimport numpy as np\n\ndef hybrid_rhs(x, u, k, theta, scale):\n    return float(u-k*x+theta*math.tanh(x/scale))\n\ndef solve(x, u, k, theta, scale):\n    return hybrid_rhs(x, u, k, theta, scale)\n",
  "p15-hybrid-steps": "import math\nimport numpy as np\n\ndef hybrid_rhs(x, u, k, theta, scale):\n    return float(u-k*x+theta*math.tanh(x/scale))\n\n\ndef hybrid_euler(x0, inputs, h, k, theta, scale):\n    states = [float(x0)]\n    for u in inputs:\n        states.append(states[-1]+h*hybrid_rhs(states[-1],float(u),k,theta,scale))\n    return states\n\ndef solve(x0, inputs, h, k, theta, scale):\n    return hybrid_euler(x0, inputs, h, k, theta, scale)\n",
  "p15-indistinguishable-rate": "import math\nimport numpy as np\n\ndef confounded_rhs(x, u, k, theta):\n    \"\"\"Linear correction: only k-theta is identified from x(t), u(t).\"\"\"\n    return float(u-(k-theta)*x)\n\ndef solve(x, u, k, theta):\n    return confounded_rhs(x, u, k, theta)\n",
  "p15-candidate-moments": "import math\nimport numpy as np\n\ndef weighted_prediction(states, weights):\n    weights = np.asarray(weights,dtype=float)\n    values = np.asarray(states,dtype=float)\n    weights = weights/weights.sum()\n    mean = weights @ values\n    variance = weights @ ((values-mean)**2)\n    return [float(mean),float(variance)]\n\ndef solve(states, weights):\n    return weighted_prediction(states, weights)\n",
  "p15-capstone-small-gradient": "import math\nimport numpy as np\n\ndef scalar_loss_grad(x, y, w, b, v, c):\n    z = w * x + b\n    a = math.tanh(z)\n    prediction = v * a + c\n    error = prediction - y\n    factor = error * v * (1 - a * a)\n    return prediction, .5 * error * error, [factor * x, factor, error * a, error]\n\ndef solve(x, y, w, b, v, c):\n    return scalar_loss_grad(x, y, w, b, v, c)\n",
  "p15-capstone-forward-gradient": "import math\nimport numpy as np\n\ndef euler_forward_sensitivity(x0, k, h, steps):\n    \"\"\"Return state and dx/dk for the *discretized* Euler program.\"\"\"\n    states, sensitivity = [float(x0)], [0.]\n    for _ in range(steps):\n        x, s = states[-1], sensitivity[-1]\n        states.append((1-k*h)*x)\n        sensitivity.append((1-k*h)*s-h*x)\n    return states, sensitivity\n\ndef solve(x0, k, h, steps):\n    return euler_forward_sensitivity(x0, k, h, steps)\n",
  "p15-capstone-scaled-loss": "import math\nimport numpy as np\n\ndef scaled_total_loss(parts, data_scale, physics_scale, boundary_scale, weights):\n    scales = [data_scale,physics_scale,boundary_scale]\n    return float(sum(w*(value/(scale*scale)) for value,scale,w\n                     in zip(parts,scales,weights)))\n\ndef solve(parts, data_scale, physics_scale, boundary_scale, weights):\n    return scaled_total_loss(parts, data_scale, physics_scale, boundary_scale, weights)\n",
  "p15-capstone-independent-error": "import math\nimport numpy as np\n\ndef rollout_metrics(truth, prediction):\n    \"\"\"Exclude the shared initial state from the mean squared rollout error.\"\"\"\n    diff = np.asarray(prediction[1:],dtype=float)-np.asarray(truth[1:],dtype=float)\n    return [float(np.mean(diff*diff)),float(abs(diff[-1]))]\n\ndef solve(truth, prediction):\n    return rollout_metrics(truth, prediction)\n"
}
