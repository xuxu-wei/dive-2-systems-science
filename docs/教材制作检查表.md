# 教材制作检查表

本表用于编写与发布已有教学内容，依据 [AGENTS](../AGENTS.md)、[教材设计](教材设计.md#lesson-standard)和[网页设计规范](网页设计规范.md)。检查的是读者实际看到和使用的成品，不把设计文档或文件存在当作教学验收。

## 1. 内容位置

- [ ] “学习目标与先修知识”仅包含能够学会的知识/能力、直接先修及复习入口。没有网页安排、安装命令、内核说明、运行时间、版本、实现状态或验收提示。
- [ ] 问题、模型假设、推导、代码、对照实验和结果解释各归其位。必要导入代码出现在第一次使用前，算法步骤在 Notebook 中可见。
- [ ] 随机网络或多主体对照中，编号与空间位置若无物理意义，不应悄悄决定保留端点、初始事件或固有参数分配；结构比较核对起点、参数分配和抽样流是否配对，单次动画只解释当前一次实现。
- [ ] 环境和打开方式集中在 [README](../README.md#run-m1) 与[学习指南](学习指南.md)；制作决策在设计文档；进度与证据在 ROADMAP。学习正文不向读者交代开发计划。

- [ ] 第 4 篇扩展按[大纲](04-非线性动态行为.md)核对定义、维数条件、Lorenz 有界性恒等式、短时收敛、截面与切向增长；新区分和新题未正式交付前不计学习完成，原五章记录不覆盖新增目标。

- [ ] 连读问题、假设、推导、实验与解释：条件位于实际使用处，删句后指代和过渡完整；无重复免责、范围自述或作者自辩，不以新增提示框承接冗余说明。对照由学习目标决定，不逐节强制增加失效案例。
- [ ] 科学审查修订后检查整段叙述；跨语言随机流、内部回归及交付口径保留在维护材料。必要的实验配置和数据用途随实验解释，学生任务不因维护检查增加。

## 2. 练习成品

- [ ] 优先替换重复公式题：早期查单位与收支，中期辨认对照中是否只改变一个因素，后期区分数值、抽样和模型误差。按先修从读图选择、小表计算到短函数逐步推进；维护回归可严格，学生必做量不因此膨胀。

- [ ] 每题明确为单选、多选或 Python 计算；前两者均属于选择题。概念、实现和迁移是教学目标，不是额外题型。
- [ ] 选择题具有完整选项、唯一正确集合和逐项解析；代码题具有函数契约、单位、输入输出、公开样例、允许库、容差及实际限制。
- [ ] 每道代码题在题面给出带类型的 `def solve(具名参数)`，并逐项解释含义与单位；所求结果、返回类型/顺序、规则、约束与样例解释分开呈现。不能用 `payload`、通用 `data` 或可变参数代替参数设计。
- [ ] 实际复制代码模板并按展示的具名参数调用；参考解、样例、补充测试和判题器一致，没有旧字典接口或临时适配器。单一结果直接返回标量/列表，多项结果的命名与顺序明确。自动检查拒绝包装接口及签名漂移。
- [ ] 函数接口变化后升题目版本；旧草稿可找回，旧记录保留，新题不沿用旧通过状态，旧页面提交收到版本更新提示。
- [ ] Notebook 内保留自洽题面，每题有“作答”链接，直接进入相同题号和版本的页面；不能用目录、未来入口、源码或仅答案文档替代。
- [ ] 题面、页面、参考解与测试使用同一版本。以 `exercises/<篇目录>/questions.json` 为公开题面来源，通过 `lesson_exercises.render_exercises` 生成 Notebook 题面，避免双份题面漂移。
- [ ] 实际尝试正确/错误选项、样例/完整提交和错误代码。解析、失败差异及提交快照能帮助纠错；样例通过不计完整完成，取消、系统错误和写入失败不误记成绩。

篇末综合另按[分层评分规范](教材设计.md#capstone-assessment)检查：

- [ ] 由易到难排列，逐题目标、分值、独立输入及核验材料齐全；分层、目标与总分合计一致。章内习题没有随综合评分调整。
- [ ] 实际完成“首次答错 → 订正通过”，确认首次分保留、练习分增加且不重复累计；样例、取消、故障、未保存不计分，实质改题的旧版本隔离。
- [ ] 首轮未完成、基础缺口、全组完成、历史记录与损坏记录均按规范显示；分层/目标明细、答题与复习链接可用，刷新及重启后结果一致，原有个人记录保持不变。

## 3. 链接打开的对象

| 入口 | 预期结果 | 不合格结果 |
|---|---|---|
| Notebook → 可视化 | 本机 HTTP 地址在浏览器显示渲染页面 | IDE 打开相对 `.html` 文件、HTML 源码、下载文件 |
| Notebook → 某道练习 | 对应题号的实际答题界面 | 空页、泛目录、仅核验提示、题号不匹配 |
| 网页 → Notebook | 系统默认 IDE 打开项目原 `.ipynb` | 浏览器源码/Notebook、下载副本、无反馈 |

- [ ] 从渲染后的 Notebook 实际点击链接，不只检查字符串或文件存在。记录地址、题号/页面标题与打开结果。生成 Markdown 链接时分别编码路径与题目参数；用实际渲染器核对含空格、括号及已编码路径的完整 href，不能只检查原始字符串。
- [ ] HTML 响应是 `text/html`，脚本能完成初始化；直接打开、刷新深链接与往返导航都有效。
- [ ] 点击播放后时间、存量高度和轨迹实际变化；暂停保持不变，重播可用；加载或本机连接失败给出恢复说明。PC 鼠标与键盘均可操作。

## 4. 专业术语

- [ ] 每个独立 Notebook、概念页及可直达题目的正文首次使用专业术语时，按[术语规范](教材设计.md#terminology-style)标注英文全称与必要的通行缩写；学习目标中的首现也算，标题和导航无需长括注。
- [ ] 首次引入解释概念、单位、用途及适用条件；英文括注不能替代教学解释。后文统一中文或选定的缩写。
- [ ] 扫描新引入而尚未收入[术语表](术语对照.json)的概念；查证通行写法后补入。检查正文、题面、解析与图注一致，避免自动替换损坏普通词语；人工排查嵌套重复括注及“阶跃/观测收敛阶”“时间延迟/滞后”等子串误配。
- [ ] 采用总纲指定的公共术语模块；以独立预期检查“重复数/复数模态”“系数组合/Python 数组”“秩序/矩阵的秩”“周期二值/周期二轨道”“重采样/采样率”“滞回/滞后项”。注入器只是短语匹配器，生成器与检查器共用规则不能替代语义审读；冲突或未确定语义先报告、再人工确认。
- [ ] 检查范围包含选择题逐项解析、计算题整体解析、参数与返回值说明及样例说明；题库修订后同步 Notebook。代码、公式、链接目标及已经正确的人工括注保持不变，重复执行不产生新括注。

## 5. 执行与证据

- [ ] 正文结论、打印标签、返回值和图例逐项一致；区分“存在、唯一、收敛”，明确 `None` 等退化返回值的含义。检查量纲、常数的单位及单时刻/缺测等边界，实际查看渲染公式，不能只凭代码运行通过。
- [ ] 从公开输入独立复算样例解释，核对预期输出、等号边界与判题；题目参数和返回值的单位须与正文选定的权重及损失定义一致。涉及采样时分开说明首次 A/D 前端滤波和已有离散数据的数字降采样。

在项目根目录执行：

```powershell
.\.venv\Scripts\python.exe tools/check_teaching.py
.\.venv\Scripts\python.exe tools/check_notebooks.py --part 01-看见系统 --work m3-part01 --write
.\.venv\Scripts\python.exe tools/check_notebooks.py --part 02-系统随时间演化 --work m3 --write
.\.venv\Scripts\python.exe -m pytest -q
```

第一项检查全部已交付正式题面、目标区、两类题、深链接与词表术语首现；随后按篇分别启动新内核并保存输出；pytest 检查数学、本机入口和判题异常。新增篇时使用相应目录，不把检查范围固定为前两篇。JavaScript 模型与共用组件另运行 `node --test tools/tests/*.test.mjs`。按改动范围选择检查，不为单纯文档调整新增程序测试。

程序检查通过后，人工查看 Notebook 正文、公式和图，完成上表的实际点击。将检查环境、范围、结果、未覆盖事项及交付文件摘要记录到 `docs/validation/`，再更新 ROADMAP 与 README。模拟数据、个人草稿、作答记录及临时预览不进入同步内容。

## 6. 内容生命周期、目录与交互回归

- [ ] 独立导论的目录、三个页面、Notebook 与第一篇链接可用；自测按[教材设计](教材设计.md#part-outlines)单独汇总，正文编号、先修、原题身份与历史成绩不受影响；旧目录未包含导论时仍可正常读取。

- [ ] 正式学习入口无样章依赖；退役内容的路由与导航同步移除，个人历史记录保留且不误计当前进度，历史证据链接到固定版本。
- [ ] 篇章文件夹包含内容名称，不仅是 m1/m2；每节 3—5 题，重难点更多，记录题目对应的知识或常见误解。
- [ ] 从导览、可视化、练习三个页面确认同一导航与布局，当前祖先展开、选中项可见；直接深链接、刷新与前进后退仍定位正确。题目和章节标题可理解，不出现 S01/E01 等内部编码。
- [ ] 章导览显示目标、重难点和知识体系；重难点来自大纲的独立学生字段，不读取“核验与反馈”或测试答案。未交付正式内容明确标注，不将设计导览计为正式教学完成。
- [ ] 全书的每张篇卡片有“第 N 篇”，各篇的每张章卡片有“篇.章”，编号、名称、顺序与侧栏一致。检查全部篇章与总纲和有效大纲一致（导论单列，正文当前 17 篇、85 章）；首屏标题与网格紧凑，没有固定高度造成的空卡片、过大间距或长标题截断。
- [ ] 用真实作答检查灰色未开始、橙色进行中、浅蓝色已完成；跨页/刷新保持一致。空题组、样例通过、取消、写入失败或旧版本不能冒充完成；样章不改变正式篇章进度。
- [ ] 代码实际输入检查 Tab、Shift+Tab、多行选区、Enter、Esc 后 Tab，以及行首 Backspace 的四空格、部分缩进与 Tab 删除；普通文本不整段误删。实际检查括号补全、跳过已有闭括号、空括号成对删除和匹配标记；Python 关键字、字符串、数字与注释有可读的 Monokai 高亮。检查撤销/重做、中文输入、切题和刷新保留草稿、恢复模板可撤销。
- [ ] 真实点击“用示例试算”和“提交判定”：结果在代码区上方展开并可见，可收起/展开，继续编辑不被抢焦点。提交缺少冒号、缩进错误、运行异常和嵌套调用代码，核对 traceback 的源码、行号、箭头、调用链及复制；定位后光标和行标记对应源码，修改草稿后停用旧行号定位并说明结果属于上次提交。旧记录没有 traceback 时仍正常显示，系统故障不伪装成语法错误。
- [ ] 目录可隐藏、展开、鼠标拖宽和键盘调宽，双击/Enter 恢复默认；跨页及刷新保留偏好，窄窗口仍能恢复目录。题目/答题双栏可调节比例，代码区随窗口扩大；各页面主体填满目录以外的可用宽度，无固定宽度造成的大片右侧空白。
- [ ] 使用长代码、长标识符与长输出检查默认自动换行及关闭换行。开关、拖动、复制和下载不修改源码字符或 Python 缩进；单栏时编辑器仍全宽，提交按钮可达。目录变化后图表、标签与动画按实际容器重绘，两栏滚动互不干扰。
- [ ] 输入样例和输出样例各有含义/单位/数据表及 Python 代码；输入含构造与调用，输出含预期结构与原始精度数值。实际复制并运行所有计算题示例，对照题面容差；网页和 Notebook 一致，复制不覆盖草稿，没有整块协议 JSON 替代解释。
- [ ] 展开最大样例，表格和代码限高且内部滚动，代码至少 14 px，不遮题干或作答控件；复制有结果反馈，复制失败仍可手动选取。“用示例试算”的用途和不计完成明确。
- [ ] 成对示例至少往返两次；按钮显示下一步目标，说明显示当前参数；穿插手动改参、减小步长和重置，标签、图形、读数保持一致。2.4 的负值→正值→负值应分别出现第一步 −5、2.5、−5 U，连续模型不变。
- [ ] 错误选择提交后为红字、透明红底；正确、系统错误、取消具有相应不同语义。切换/hover/选中反馈平滑且无持续跳动，减少动画设置有效。
- [ ] PC 1024、1366、1440、1920、2560 宽及短窗口检查主要入口、图形和代码可读性，覆盖目录开关与宽度极值；实际播放/暂停/重播，实际点击 Notebook 默认 IDE 入口。按本轮改动记录实际覆盖事项，不把历史验收写作本轮运行。

## 7. 开始首页与知识星图

以下检查针对[首页规范](网页设计规范.md#knowledge-home)，均须记录本轮实际结果；新增首页不改变教学章节的交付状态。

- [ ] `/` 显示“开始”，`/catalog/` 显示全书目录；侧栏顺序一致且当前入口选中。首页、导览、可视化和练习中的左上角 logo 均返回首页，章节“目录”面包屑返回全书目录；删除顶端冗余标题/图例后，各篇章进度仍存在且可读。
- [ ] 首页顶端只有居中的“动手学系统科学”大标题，其下居中显示全景/当前篇/当前章导航；原左上口号、英文装饰标题与重复简介已删除。开关/拖宽目录及缩放窗口时，标题、层级导航、信息卡围绕可用画面居中；卡片文字居中、宽度不超过 560 px，以“第 x / xx 篇（章）”表示位置，左右按钮首尾循环并同步选中节点。全书目录位于探索指南之后；五论图例在底部中央标签下方，无旧底栏操作提示与分隔线。控件不遮挡星图及标签。
- [ ] 全景、篇视图和章视图分别只选择篇、章、知识点；单击、Tab 聚焦只选中并切换卡片，双击、Enter 或滚轮放大才进入下一级；滚轮缩小返回。层级过渡在 0.5—1 秒内完成，镜头与材质连续变化，低帧率不累计慢放；返回按钮、面包屑与浏览器前进后退按页面约定恢复层级和对应选中节点。节点名称、编号、目录归属与当前有效大纲一致，不能混入退役样章。
- [ ] 进入篇后，当前星系成为显著大于周围章星球的中心主体；进入章后，该章星球放大，知识点呈更小的小行星或光点。逐层进入/返回能识别同一对象的连续变化，篇章内部不仍以全景黑洞作为中心，不以同尺寸、同材质圆点代替层级。
- [ ] 已发布知识点准确映射到具体小节，悬停/聚焦提示学习入口；双击知识点或点击卡片中的学习按钮使用默认 IDE 打开原 Notebook，记录成功请求与失败反馈，不导航到源码或下载副本。未发布节点清楚标识并仅提供真实存在的设计导览。
- [ ] 五论名称对照郭雷 2016 年原文印刷页 293，分类、混色和空间位置注明为本站组织方案。节点主体色相由类别决定，素材只贡献明暗/结构，不将类别全部染成贴图原色；对照网页规范的两套五色色值检查图例、悬浮色点、远近景及 WebGL/Canvas，单类别无额外混蓝，多类别使用相同调和规则；检查切层/刷新及强弱光照下的类别辨识，原教学图表色板不受影响。
- [ ] 连续观察总览小星系沿与中心吸积盘一致的扁椭圆缓慢公转，轨道朝向、倾斜与整体空间一致；不是只旋转各自贴图。远景有稳定种子决定的椭圆弥散、扁盘尘带、紧核、不规则、双团等多形态，不能找到全体由同一图旋转克隆的模式，也不提前呈现清晰旋臂；进入篇才逐渐揭示精细旋臂，返回仍识别同一星系。
- [ ] 黑洞视界在静止镜头下轮廓清楚稳定，吸积盘光流实际变化；悬停/聚焦暂停后公转、局部旋转和光流保持不动，恢复无跳变。篇内星球在不同方位以及拖动后，亮面均朝向中心星系，光源色温一致且类别色可辨。相关说明明确是课程空间隐喻，不将画面或材质效果写成宇宙物理仿真已验证。
- [ ] 默认镜头连续观察数秒即可看出吸积盘流动、星系自转与公转；深色背景无可辨方块网格或色阶接缝。实际检查 PC 计算字号：操作/进度/说明至少 14 px，节点编号/图例至少 13 px；放大后卡片、中央标签、节点与底栏无重叠，悬浮提示完整位于场景内。
- [ ] 抽查课程直接先修、同节关联与篇级汇总的边来源。高权重连边总体拉近，排斥避免重叠；布局不宣称精确物理意义。真实轻拖节点观察关联单元位置响应，释放后不误点击、不越界；快速连续拖动仍稳定。
- [ ] 真实拖动空白区仅平移视图，拖动节点仅改变节点关联布局；释放不会误点击。滚轮跨阈值逐层进入/返回，连续惯性滚动与阈值反复小幅滚动不穿越多层、不抖动、无目标不随机进入；章内滚轮不会打开 Notebook。控件/信息卡滚动不驱动镜头，标签、连线与命中区域在平移/缩放后仍对齐；缩放、重置和键盘平移可重复使用。
- [ ] 真实悬停与键盘聚焦仅高亮节点、直接边、直接邻居，邻居亮度低于焦点，非直接节点不被传递高亮；名称与提示可读。悬停/聚焦时旋转停止，离开后按当前暂停状态恢复；成就光晕与临时高亮独立，浅色主题高亮仍清楚。
- [ ] 在独立浏览器学习配置中检查首次开场有实际画面和镜头变化，结束聚焦合理的续学篇并显示说明；零进度、存在进行中篇、已发布题目全通过和部分内容待交付各有正确结果。不用打开页面或播放动画代替完成练习。
- [ ] 开场中跳过、结束后重播、连续暂停/恢复、快速切层均反复操作；切到后台再回来没有时间跳跃或重复加速。减少动态效果开启后默认稳定且入口完整，主动重播仍可跳过；初始化失败不会留下点击无反应的按钮。
- [ ] 用独立测试记录核对有效通过比例、篇末练习成绩和光辉强度，直接展示分母与成绩来源；章/概念没有独立成绩时不显示虚构分数。未交付范围、空题组、旧版本、样例、取消、未保存与损坏记录不造成虚假完成；真实个人记录文件不被修改。
- [ ] 本机进度接口断开、星图数据失败或画布不可用时，能看到明确说明并进入全书目录。数据恢复后重新读取，不把读取失败显示为真实零分；已加载静态内容保持可读。屏蔽外网后本地资源和正常学习入口仍工作，不请求 CDN。
- [ ] Three.js、GPU 材质与生成星系贴图均由本地资源加载，依赖来源/固定版本/许可证及贴图来源说明齐全。分别检查深浅主题与近远镜头的星系结构、行星表面、光晕和文字，无方形贴图边、透明接缝、过曝色团或切层闪白。记录实际 PC 窗口、像素密度及性能观察，不仅证明已导入图形库。
- [ ] 验证 WebGL 不可用、纹理失败、材质初始化失败或上下文丢失的降级：仍可选择/逐层探索、读取信息、平移缩放和进入学习；若全部绘图不可用，目录回退始终可操作。降级前后层级、主题和有效进度一致，不保留假可用的动画控件，不把回退画面计作 GPU 路径已验收。
- [ ] 从首页切换深色/浅色，依次进入目录、篇章、练习与可视化，再返回首页并刷新；在普通页再次切换并反向验证，所有页面保持同一主题。旧首页偏好迁移、直接进入教学页、多标签变更及浏览器禁止存储分别检查；主题切换不清空草稿、作答记录或图表参数。
- [ ] 深浅主题分别检查信息卡、目录卡片、题面、选项、结果、表格、编辑器上方控件、类别说明及 logo 的对比度。教学图表外围面板随主题变化，限定的白色绘图区保持深色坐标和原数据颜色，不使用全页反色；Monokai 高亮、traceback 与错误红字/透明红底仍正确。目录收缩/拖宽及 PC 1024、1366、1440、1920、2560 px、短窗口下，画布、节点与信息卡同步重排，主要控件不被遮挡。
- [ ] Tab、Enter/Space、Escape 与可见返回按钮可完成逐层探索，键盘聚焦能获得悬停同等信息，无焦点陷阱；画布不重复朗读，节点有语义名称，状态提示不按动画帧反复播报。截图、实际点击和自动检查各记录其覆盖范围，不以函数测试替代交互验收。
