# 学习入口自动启动验收

2026-09-23；分支 `codex/learning-20260922`。根目录提供 `开始学习.cmd`，VS Code 工作区使用打开时自动任务；206 本 Notebook 各有一个带源码隐藏元数据的服务初始化单元，网页仍由原有 `tools/serve.py` 提供。

- 从未监听的 8000 端口运行根目录入口：首页、导论练习页、全书目录均返回 `200` 和 HTML；再次运行复用原服务。
- 教材服务记录工作区和内容指纹；实测内容变化后自动重启，正在判题时拒绝重启，其他程序占用端口时拒绝接管。
- 三本导论 Notebook 在全新内核顺序运行通过；关闭服务后，在 Notebook 内核运行初始化单元可访问练习页。内部 HTML 预览已过滤启动单元。
- 结构与同步检查覆盖 206 本 Notebook、939 道题；原有单元、输出和元数据未改。Python 全量测试 1044 项、网页 JavaScript 测试 151 项通过；最终启动修订另做定向复验。

使用边界：VS Code 首次需信任工作区并允许自动任务。单独打开 Notebook 时先顺序运行单元格；在验证器中关闭临时内核会结束由该内核启动的服务，之后继续使用网页可从根目录入口启动。Jupyter 源码隐藏元数据和内部 HTML 预览已检查；不同 IDE 对该元数据的呈现尚未逐一做界面验收。`AGENTS.md`、模拟数据和学习记录未改动或纳入同步。
