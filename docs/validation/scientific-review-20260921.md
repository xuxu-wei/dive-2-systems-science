# 2026-09-21 科学性审查报告复核与勘误

依据外部《科学性审查报告》（审查日期 2026-09-20—21）逐条核对当前教材、题库、参考解和网页。报告是问题线索，不替代本项目的复核与运行验收；未列问题的内容不据此声称完全正确。

| 编号 | 复核判断与修订 | 核验证据 |
|---|---|---|
| 01 Euler 非负性 | **成立，判题计算错误。** 旧实现把连续矩阵非对角元的 `1e-10` 容差直接用于任意步长。现对完整 `I+hA` 逐项检查；`closed_exchange` 仍独立按连续矩阵判断。同步题面、参考解和两本 Notebook，题目版本升至 2；增加报告中的大步长反例。 | 反例一步矩阵 `[[11,-10],[-10,11]]`，新函数返回连续标志 `True`、Euler 标志 `False`；旧解在真实判题中为 `WA`，新解通过全量判题。14.5.1 与篇末综合重新执行。 |
| 02 连续／离散能控能观 | **成立，公式正确而时间解释不准。** 连续模型的 `AB` 改为输入经过状态间耦合产生的方向；`CAx` 解释为无输入时的读数变化率，已知输入需扣除 `CBu`。习题把连续变化率矩阵与离散一步矩阵分别说明；网页使用的离散观测例子也标明身份。 | 10.3.1、10.4.1 Notebook、`p10-controllability-two` 题面与控制网页；矩阵和判题答案不变。参见[连续 LTI 能观矩阵说明](https://hankyang.seas.harvard.edu/OptimalControlEstimation/app-lti-system-theory.html)。 |
| 03 扩散候选的负值 | **成立，变量物理含义不清。** `φ` 说明为相对均匀背景的浓度偏差，第一项振幅以 `1 U/V` 记数；负值只表示低于背景。同步 `p15-diffusion-profile` 返回说明、样例解释、Notebook 和网页；原方程与数值结果保留。 | 样例末点 `−1` 现解释为偏差，未称为负的绝对浓度。 |
| 04 零交换率边 | **成立，需明确连通性约定。** 只把正交换率边用于判断连通分量；零权边等同关闭通道。 | 三节点 `1,0` 边权的谱为 `[0,0,2]`，长期状态趋向 `[1,1,6]`；Notebook、网页说明和独立测试一致。 |
| 05 零增益相位 | **成立，数值约定不能充当物理相位。** 频响题限定 `K>0`，版本升至 2；Notebook 明写 `K=0` 时幅值为零、相位无定义，也没有有限 dB 幅值。网页滑块原本已限定正增益。 | 题库、Notebook 与网页输入范围一致；[NumPy `arctan2` 的零输入数值约定](https://numpy.org/doc/stable/reference/generated/numpy.arctan2.html)不改变零复数的相位含义。 |
| 06 群体序参量 | **成立，正确选项用词过强。** 改为“整体较集中，但个别振子仍可能明显偏离”，保留不据此宣称长期锁频的边界。正确选项集合仍为 A、C，题目版本不变。 | 19 个零相位与 1 个反相振子可有 `R=0.9`；题库、解析与篇末 Notebook 同步。 |
| 07 基础公式下标 | **成立，TeX 源式损坏。** 修复第 1 篇三处新旧、初末下标；收支公式与计算不变。 | 重新读取 Notebook 源确认 `t_{\mathrm{new}}`、`A_{\text{末}}`、`B_{\text{末}}`、`C_{\text{末}}`。 |

验收：203 本 Notebook 与 930 道题的题面、链接、首次术语标注检查通过；第 1、10、11、14、15 篇 Python 专项共 115 项通过，另新增旧错误实现被真实判题拒绝的专项测试通过；相关 20 项网页模型 JavaScript 测试通过。实际重新执行两本代码受影响的第 14 篇 Notebook；纯文字和公式修订不冒称重新运行全书。题目版本变更保留旧作答记录，旧版本结果不计入新版进度。`AGENTS.md`、模拟数据和个人作答记录均未改动。
